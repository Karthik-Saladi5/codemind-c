#include<stdio.h>
int main()
    {
        int t,i,x;
        scanf("%d",&t);
        for(i=1;i<=t;i++)
        {
            scanf("%d",&x);
            if(x>30) printf("YES");
            else printf("NO");
            printf("
");
        }
    }